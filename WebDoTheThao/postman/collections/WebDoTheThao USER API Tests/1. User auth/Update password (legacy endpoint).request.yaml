$kind: http-request
url: "{{baseUrl}}/api/auth/update-password"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "password": "{{userPassword}}"
    }
order: 7000
