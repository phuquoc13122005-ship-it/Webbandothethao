$kind: http-request
url: "{{baseUrl}}/api/auth/forgot-password/reset"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{userEmail}}",
      "code": "{{resetCode}}",
      "password": "{{userPassword}}"
    }
order: 12000
